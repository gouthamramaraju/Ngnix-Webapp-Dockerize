# Nginx Webapp Dockerize

This project demonstrates how to dockerize a web application and use Nginx as a reverse proxy to manage incoming traffic.

## Project Structure
* **app/** - Contains the web application source code and its Dockerfile.
* **nginx/** - Contains the Nginx configuration and Dockerfile.
* **docker-compose.yml** - The configuration file to run both containers together.

